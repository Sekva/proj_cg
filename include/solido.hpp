#ifndef SOLIDO_HPP
#define SOLIDO_HPP

#include <iostream>
#include <vector>
#include <fstream>
#include <sstream>
#include <iterator>


typedef long double Num;

struct vertice;
typedef struct vertice Vertice;

struct face;
typedef struct face Face;

struct face_vertice;
typedef struct face_vertice Objeto;

#endif
